# Public site URL — used for canonical, OG, sitemap, robots
NEXT_PUBLIC_SITE_URL=https://buildxworks.com

# Resend (https://resend.com) — required for the lead form to deliver emails.
# Without these, leads still log to server console (console.log "[lead] ...")
# but no email is sent.
RESEND_API_KEY=
LEADS_EMAIL=raju.raman@ourworldenergy.com
LEADS_FROM=BuildXWorks <onboarding@resend.dev>
